# App Store Connect API Key (recommended over password)
app_identifier("com.sillyl12324.comfy-portal")
apple_id("2295448729@qq.com")
team_id("QUVB58SSQ7")

# For CI/CD, use environment variables instead:
# ENV["APP_STORE_CONNECT_API_KEY_KEY_ID"]
# ENV["APP_STORE_CONNECT_API_KEY_ISSUER_ID"]
# ENV["APP_STORE_CONNECT_API_KEY_KEY"]

for_platform :ios do
  app_identifier("com.sillyl12324.comfy-portal")
end

for_platform :android do
  # CI: use decoded file from GitHub Secrets; Local: use home directory
  ci_json_path = File.expand_path("~/.google/play-store-credentials.json")
  json_key_file(ci_json_path) if File.exist?(ci_json_path)
  package_name("com.sillyl12324.comfyportal")
end
